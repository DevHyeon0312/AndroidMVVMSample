<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devhyeon.mvvmSample" >

    <!--
    Created By DevHyeon on 2020.11.21
    DevHyeon Blog : https://www.devhyeon.com
    DevHyeon Git  : https://github.com/DevHyeon0312
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!-- Main Activity : Calc Main -->
        <activity android:name=".Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Usage Activity : Calc Usage -->
        <activity android:name=".Usage.UsageActivity"/>

    </application>

</manifest>